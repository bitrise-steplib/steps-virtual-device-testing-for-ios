format_version: "11"
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

include:
- repository: steps-check
  branch: master
  path: steps.bitrise.yml

workflows:
  # check: included from steps-check/steps.bitrise.yml

  e2e:
    steps:
    - git::https://github.com/bitrise-steplib/steps-check.git:
        inputs:
        - workflow: e2e

  sample:
    envs:
    - TEST_APP_URL: https://github.com/bitrise-io/sample-swift-project-with-parallel-ui-test.git
    - TEST_APP_BRANCH: master
    - BITRISE_PROJECT_PATH: BullsEye.xcworkspace
    - BITRISE_SCHEME: BullsEye
    - TEST_DEVICES: iphone16pro,18.3,en,portrait
    steps:
    - script:
        title: Clean _tmp dir
        inputs:
        - content: |-
            #!/bin/env bash
            set -ex
            rm -rf ./_tmp
    - git::https://github.com/bitrise-steplib/bitrise-step-simple-git-clone.git:
        inputs:
        - repository_url: $TEST_APP_URL
        - branch: $TEST_APP_BRANCH
        - clone_into_dir: ./_tmp
    - xcode-build-for-test:
        inputs:
        - project_path: ./_tmp/$BITRISE_PROJECT_PATH
        - scheme: $BITRISE_SCHEME
        - test_plan: FullTests
        - output_tool: xcodebuild
        - automatic_code_signing: api-key
        - certificate_url_list: $BITRISE_APPLE_APPLE_CERTIFICATE_URL_LIST
        - passphrase_list: $BITRISE_APPLE_APPLE_CERTIFICATE_PASSPHRASE_LIST
    - path::./:
        inputs:
        - test_devices: $TEST_DEVICES
        - download_test_results: "true"

  maintenance:
    steps:
    - script:
        title: Compare step.yml's device tables with the live catalog
        inputs:
        - content: |-
            #!/bin/env bash
            set -eo pipefail
            # Needs the gcloud CLI, and either an already signed in account or
            # $SERVICE_ACCOUNT_JSON. The tag keeps it out of `go test ./...` in check.
            go test -v -tags maintenance ./maintenance

  generate_readme:
    steps:
    - git::https://github.com/bitrise-steplib/steps-readme-generator.git@main: { }
